<!--

    External Disk Cache configuration schema.

    Please do not edit this file. To add configuration sections to the schema,
    put them in .xml files similar to this one, in this directory. They will
    be picked up automatically at application pool startup.

-->

<configSchema>

  <!--

    These settings are always access from the top-level
    <system.webServer/webdav> section.

  -->

  <sectionSchema name="system.webServer/externalCache/diskCache">
    <collection addElement="add">
      <attribute name="name" type="string" required="true" isUniqueKey="true" />
      <attribute name="path" type="string" required="true" />
      <attribute name="userName" type="string" />
      <attribute name="password" type="string" caseSensitive="true" encrypted="true" defaultValue="[enc:AesProvider::enc]" />
      <attribute name="cleanupPeriod" type="timeSpan" defaultValue="00:05:00" validationType="timeSpanRange" validationParameter="0,4294967,1" />
    </collection>
  </sectionSchema>
</configSchema>
